import ProfilePage from "@/components/pages/ProfilePage";

export const metadata = {
  title: "Francisco Guevara — Profile",
  description:
    "I build autonomous AI that works inside real operations — a private digital twin, an always-on agent workforce, and the tools around them.",
};

export default function Page() {
  return <ProfilePage />;
}
